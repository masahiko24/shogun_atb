<header class="headewrapper">
        <nav>
         <% if user_signed_in? %>
        <ul class="main-nav">
        <li ><%= link_to "マイページ", "/users/#{current_user.id}",class: "sign"  %>
        <li ><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class:"sign" %>
        <li ><%= link_to '投稿する', new_photo_path, class: "sign" %></li>
         </ul>
         <% else %>
        <ul class="sub-nav">
        <li ><%= link_to '新規登録', new_user_registration_path, class: "sign" %></li>
        <li ><%= link_to 'ログイン', new_user_session_path, class: "sign" %></li>
        <li ><%= link_to 'Shougun', root_path, class: "top" %></li>

        </ul>
        <% end %>
        </nav>

        
  </header>